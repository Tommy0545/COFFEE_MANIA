<!DOCTYPE html>
<html>


  <head>

    <title>COFFEEMANIA</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", :media => "all" %>
    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
  <div class="background-image">
  <div class="body">
    <header class="shadow-sm">
      <nav style="bg-#663300">
      <nav class="navbar navbar-expand-lg">
        <div class="container">
          <h2 class="main-title"><i class="fa-solid fa-mug-hot" style="color: white;"></i><%= link_to "COFFEEMANIA",root_path,class: 'text-light' %></h2>
          <div class="collapse navbar-collapse" id="navbarNavDropdown">
            <ul class="navbar-nav ml-auto">

            <% if user_signed_in? %>
              <li class="nav-item m-1 rounded border border-muted">
                <%= link_to 'My Page', user_path(current_user.id), class: 'nav-link text-light' %>
              </li>
              <li class="nav-item m-1 rounded border border-muted">
                <%= link_to 'Cafe', cafes_path, class: 'nav-link text-light' %>
              </li>
              <li class="nav-item m-1 rounded border border-muted">
                <%= link_to 'Beans', beans_path, class: 'nav-link text-light' %>
              </li>
              <li class="nav-item m-1 rounded border border-muted">
                <%= link_to 'Log out', destroy_user_session_path, method: :delete, class: 'nav-link text-light' %>
              </li>
            <% else %>
              <li class="nav-item m-1 rounded border border-muted">
                <%= link_to 'Sign up', new_user_registration_path, class: 'nav-link text-light' %>
              </li>
              <li class="nav-item m-1 rounded border border-muted">
                <%= link_to 'Log in', new_user_session_path, class: 'nav-link text-light' %>
              </li>
              <li class="nav-item m-1 rounded border border-muted">
                <%= link_to 'Top', root_path, class: 'nav-link text-light' %>
              </li>
              <li class="nav-item m-1 rounded border border-muted">
                <%= link_to 'About', about_path, class: 'nav-link text-light' %>
              </li>
            <% end %>
            </ul>
          </div>
        </div>
      </nav>
     </nav>
    </header>
    <main class="container mt-5">
        <%= yield %>
    </main>
  </div>


    <div class="footer">
    　 <div class="footer-text">\ Coffee makes everyone Happy /</div>
    </div>

 </div>
 </body>
</html>